; Script generated by the Inno Setup Script Wizard.
; SEE THE DOCUMENTATION FOR DETAILS ON CREATING INNO SETUP SCRIPT FILES!

#define MyAppName "IsyStats"
#define MyAppPublisher "Dennis Hempel"
#define MyAppURL "https://skoosoft.de"
#define MyAppExeName "IsyStats.exe"
#define MyAppSourceFolder "bin\Publish"
#define ApplicationVersion GetFileVersion(MyAppSourceFolder + '\' + MyAppExeName)

[Setup]
AppId={{0A78D18A-C402-48B5-BC85-3839BF1AA359}
VersionInfoVersion={#ApplicationVersion}
AppName={#MyAppName}
AppVersion={#ApplicationVersion}
AppVerName={#MyAppName}
AppPublisher={#MyAppPublisher}
AppPublisherURL={#MyAppURL}
AppSupportURL={#MyAppURL}
AppUpdatesURL={#MyAppURL}
DefaultDirName=c:\programdata\IsyStats
DisableProgramGroupPage=yes
SetupLogging=yes
OutputDir=bin
OutputBaseFilename=IsyStats.Setup
Compression=lzma2/normal
SolidCompression=yes
UninstallDisplayIcon={app}\{#MyAppExeName}
PrivilegesRequired=admin
CloseApplications=force
RestartApplications=yes

[Languages]
Name: "german"; MessagesFile: "compiler:Languages\German.isl"

[Files]
Source: "{#MyAppSourceFolder}\*"; Excludes: "appsettings.json"; DestDir: "{app}"; Flags: ignoreversion recursesubdirs;

[InstallDelete]
;Type: files; Name: "{app}\*.dll";

[Dirs]
Name: "{app}"; Permissions: users-full

[Run]
Filename: "{app}\{#MyAppExeName}"; Parameters: "-install -silent";

[UninstallRun]
Filename: "{app}\{#MyAppExeName}"; Parameters: "-uninstall -silent";

[Code]
procedure CurUninstallStepChanged(CurUninstallStep: TUninstallStep);
begin
  if CurUninstallStep = usPostUninstall then
  begin 
      if MsgBox('Sollen alle Programm-Einstellungen gelöscht werden?', mbConfirmation, MB_YESNO) = IDYES
      then
        begin
          DelTree(ExpandConstant('{app}'), True, True, True);
        end;
  end;
end;

function PrepareToInstall(var NeedsRestart: Boolean): String;
var
  ResultCode: Integer;
begin
    Exec('cmd', '/C net stop IsyStats', '', SW_HIDE, ewWaitUntilTerminated, ResultCode);
end;

procedure StopService();
var
  ResultCode: Integer;
begin
    Exec('cmd', '/C net stop IsyStats', '', SW_HIDE, ewWaitUntilTerminated, ResultCode);
end;  
    
procedure StartService();
var
  ResultCode: Integer;
begin
    Exec('cmd', '/C net start IsyStats', '', SW_HIDE, ewNoWait, ResultCode);
end;
